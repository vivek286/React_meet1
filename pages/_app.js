import tottal from '../styles/globals.css'
import Layout from '../components/layout/Layout'
function MyApp({ Component, pageProps }) {
  return <tottal><Layout><Component {...pageProps} /></Layout></tottal>
}

export default MyApp
